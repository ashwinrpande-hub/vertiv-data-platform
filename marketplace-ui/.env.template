# Frontend (marketplace-ui/frontend/.env.local)
# Copy to .env.local — never commit .env.local to git

# Set to "false" to hit the real FastAPI backend
VITE_USE_MOCK=true

# FastAPI local dev URL
VITE_API_URL=http://localhost:8000

# ─────────────────────────────────────────────────
# Backend (marketplace-ui/backend/.env)
# ─────────────────────────────────────────────────

SNOWFLAKE_ACCOUNT=drdwkxe-gmc96114
SNOWFLAKE_USER=CHANDANPATIL99
SNOWFLAKE_PASSWORD=your_password_here
SNOWFLAKE_WAREHOUSE=ANALYTICS_WH
SNOWFLAKE_DATABASE=VERTIV_PRODUCTS
SNOWFLAKE_SCHEMA=SALES_PERFORMANCE
SNOWFLAKE_ROLE=VERTIV_GLOBAL_ANALYST
ALLOWED_ORIGINS=http://localhost:3000
